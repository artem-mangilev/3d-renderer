/**
 * @typedef IColor
 * @prop {number} red
 * @prop {number} green
 * @prop {number} blue
 * 
 * @typedef {[number, number, number]} I3DVector
 * 
 * @typedef ISphere
 * @prop {I3DVector} center
 * @prop {number} radius
 * @prop {IColor} color
 * 
 */